#include <stdio.h>
#include <wchar.h>
int main(){
    int N, i;
    scanf("%d", &N);
    for (i = 1; i<= 10; i++){
        printf("%d %c %d = %d \n", N, 215, i, N*i);
        }
        return 0;
}